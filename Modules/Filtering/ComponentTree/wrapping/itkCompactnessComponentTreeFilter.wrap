itk_wrap_class("itk::CompactnessComponentTreeFilter" POINTER)
  foreach(t ${WRAP_ITK_SCALAR})
    foreach(d ${WRAP_ITK_DIMS})
      itk_wrap_template("${ITKM_CT${t}${d}D}" "${ITKT_CT${t}${d}D}")
    endforeach(d)
  endforeach(t)
itk_end_wrap_class()
